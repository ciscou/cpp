<nav class="top-bar" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to "Cerámica Padilla Padilla", root_path %></h1>
    </li>
    <li class="toggle-topbar menu-icon">
      <a href="#"><span>Menu</span></a>
    </li>
  </ul>

  <section class="top-bar-section">
    <ul class="left">
      <li<% if active_tab?("categories") %> class="active"<% end %>><%= link_to "Catálogo", categories_path %></li>
      <li<% if active_tab?("who") %> class="active"<% end %>><%= link_to "¿Quiénes somos?", pages_who_path %></li>
      <li<% if active_tab?("where") %> class="active"<% end %>><%= link_to "¿Dónde estamos?", pages_where_path %></li>
      <li<% if active_tab?("contact") %> class="active"<% end %>><%= link_to "Contacte con nosotros", new_contact_path %></li>
    </ul>

    <ul class="right">
      <li class="has-form">
        <%= form_tag search_products_path, method: "get" do %>
          <div class="row collapse">
            <div class="large-8 small-9 columns">
              <%= text_field_tag :q, params[:q], placeholder: "Maceta" %>
            </div>
            <div class="large-4 small-3 columns">
              <%= submit_tag "Buscar", class: "button expand" %>
            </div>
          </div>
        <% end %>
      </li>
    </ul>
  </section>
</nav>
