<% title @product.name %>
<% description @product.description %>

<h2><%= @product.name %></h2>

<p>
  <%= image_tag @product.picture_url %>
</p>

<% if @product.decoration.exists? || @product.category.decorations.any? %>
  <p>
    <% if @product.decoration.exists? %>
      <strong>La decoración de la foto es <%= link_to_decoration @product.decoration %>.</strong>
    <% end %>
    <% if @product.category.decorations.any? %>
      También disponible en las decoraciones:
    <% end %>
  </p>
<% end %>
<% if @product.category.decorations.any? %>
  <ul class="inline-list">
    <% @product.category.decorations.each do |decoration| %>
      <li><%= link_to_decoration decoration %></li>
    <% end %>
  </ul>
<% end %>

<%= simple_format @product.description, :class => "description" %>

<p><%= link_to "Volver a #{@product.category.name}", category_products_path(@product.category, anchor: @product.to_param), class: "button radius expand" %></p>
