<% title       @category.name %>
<% description @category.description %>

<% cache [ I18n.locale, :mobile, @category, :products ] do %>
<h2 data-generated-at="<%= Time.current.to_s(:db) %>"><%= @category.name %></h2>

<% if @products.any? %>
  <ul id="products" class="clearing-thumbs" data-clearing>
    <%= render partial: "product", collection: @products, as: :product %>
  </ul>
<% else %>
  <p>Lo sentimos, no se ha encontrado ningún producto en la categoría <%= @category.name %></p>
<% end %>

<%= simple_format @category.description if @category.description.present? %>

<% if @products.any? %>
  <p>
    Pulse sobre una foto para verla más grande. Podrá cambiar de foto arrastrando con el dedo. Para salir pulse en la x.
  </p>
<% end %>
<% end %>

<p><%= link_to "Volver al catálogo", categories_path, class: "button radius expand" %></p>
