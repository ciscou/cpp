<% title "Contacte con nosotros" %>

<div id="box7" class="box-style">
  <div class="title">
    <h2><span>Contacte con nosotros</span></h2>
  </div>
  <div class="content">
    <p>
    Puede contactar con nosotros a través de los siguientes medios:
    </p>
    <dl>
      <dt>Teléfono</dt> <dd>953 678 087</dd>
      <dt>Fax</dt>      <dd>953 670 902</dd>
      <dt>e-mail</dt>   <dd><%= mail_to "fabrica@ceramicapadilla.es", nil, :encode => "hex" %></dd>
    </dl>
    <p class="fill-contact-us-form">
    <% if user_signed_in? %>
      O si lo prefiere, puede rellenar el siguiente formulario:
    <% else %>
      O si lo prefiere, <%= link_to "inicie sesión", new_user_session_path %> y rellene el siguiente formulario:
    <% end %>
    </p>

    <%= form_for @contact do |f| %>
      <p>
      <%= f.label :subject, "Asunto" %><br/>
      <%= f.select :subject, Contact::SUBJECTS, { :prompt => "Por favor, elija el motivo de su consulta" }, :disabled => ! user_signed_in? %>
      </p>
      <p>
      <%= f.label :body, "Mensaje" %><br/>
      <%= f.text_area :body, :disabled => ! user_signed_in? %>
      </p>
      <p>
      <%= f.submit "Enviar", :disabled => ! user_signed_in? %>
      </p>
    <% end %>
  </div>
</div>
